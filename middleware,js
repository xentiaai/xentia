import { authMiddleware } from "@clerk/nextjs";

export default authMiddleware({
  publicRoutes: ["/"], // ścieżki dostępne bez logowania
});

export const config = {
  matcher: ["/((?!.*\\..*|_next).*)"],
};
